---
version: "3"
tasks:

  dns:
    desc: Create pod to debug dns
    cmds:
      - kubectl apply -f https://k8s.io/examples/admin/dns/dnsutils.yaml
  calicoctl:
    desc: Calicoctl tool
    cmds:
      - kubectl apply -f https://docs.projectcalico.org/manifests/calicoctl.yaml

